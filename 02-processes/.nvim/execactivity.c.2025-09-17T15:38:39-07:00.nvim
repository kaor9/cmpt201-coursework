#include <stdio.h>
#include <unistd.h>
#include <sys/types.h>

int main() {
  printf("hi!\n");

  pid_t pid = fork();
  if (pid == 0) {
    printf("child\n");
  } else  {
    printf("parent\n");
    execl("/bin/ls", "/bin/ls", NULL);
  }

  printf("end");
  return 0;
}
